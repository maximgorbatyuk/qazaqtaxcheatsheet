---
import ContentPage from './ContentPage.astro';
import regions from '../data/regions-2026.json';
---

<ContentPage title="Ставка налога по регионам">
  <p class="intro">
    Ставка подоходного налога (ИПН) для ИП на упрощенке зависит от региона. Ниже приведены известные ставки на 2026 год.
    По умолчанию ставка составляет 4%, но отдельные регионы могут установить пониженную ставку.
  </p>

  <div class="table-scroll">
    <table class="data-table" aria-label="Ставки налога по регионам">
      <thead>
        <tr>
          <th>Город</th>
          <th>Ставка</th>
          <th>Референс</th>
        </tr>
      </thead>
      <tbody>
        {regions.map((r) => (
          <tr>
            <td>{r.city}</td>
            <td class="cell-mono">{r.rate}%</td>
            <td class="cell-ref">{r.reference}</td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>

  <p class="note">
    Проверяйте свой город по ссылке:
    <a href="https://tsnik.kz/news/polnyy-spisok-regionov-so-snizhennoy-stavkoy-kpn-i-ipn-po-uproshchenke-na-2026-god/" target="_blank" rel="noopener noreferrer" class="link">Полный список регионов со сниженной ставкой КПН и ИПН по упрощенке на 2026 год</a>.
  </p>
</ContentPage>

<style>
  .table-scroll {
    margin-bottom: var(--space-5);
  }

  .cell-mono {
    font-weight: 600;
  }

  .cell-ref {
    font-size: 0.8125rem;
    color: var(--fg-secondary);
    max-width: 30rem;
    white-space: normal;
    line-height: 1.4;
  }

</style>
